{
  "name": "nmap",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.mjs",
    "build": "npx esbuild ./index.mjs --bundle --outfile='dist/index.js' --platform=node",
    "postbuild": "rm -f nmap.zip && zip -j nmap.zip dist/**",
    "predeploy": "npm run build",
    "deploy": "./deploy.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1323.0",
    "jsonwebtoken": "^9.0.0",
    "jwks-rsa": "^3.0.1"
  }
}
